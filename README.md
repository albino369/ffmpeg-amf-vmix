# ffmpeg-amf-vmix
This script compiles FFmpeg for Windows with AMD AMF support and creates a transparent proxy that automatically converts CPU-based encoding (libx264) to GPU-based encoding (h264_amf). Perfect for vMix streaming with AMD GPUs!
